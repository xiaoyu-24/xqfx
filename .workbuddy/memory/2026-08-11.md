# 2026-08-11

- 在项目根目录 `.gitignore` 末尾新增 `# Image files` 分类并添加 `*.png` 忽略规则（第 41 行）。已验证新规则生效（未跟踪的 png 均被忽略）。注意：仍有 8 个 png 文件此前已被 git 跟踪（`git ls-files` 可见），如需停止跟踪需执行 `git rm --cached`。
# 2026-08-11

- 分页组件统一改造（用户要求改代码不验证）：新建 frontend/src/components/AppPagination.vue——隐藏 antd «/» 快速翻页按钮（itemRender 渲染为静态省略号）、固定开启 show-quick-jumper（跳至 X 页）、统一圆角 8px 与聚焦光晕样式，透传全部 props/事件。已替换 7 个组件共 10 处分页：Dashboard×2、NotificationCenter、DictionaryManagement、RequirementList、UserManagement、SystemManagement×2、VersionRequirementManagement×2。未跑构建验证，由用户自行验证。
- 按用户要求进一步调整 AppPagination：彻底移除 «/» 快速翻页位置（不再渲染省略号占位），页码截断处无任何元素，仅保留页码 + 前后翻页 + "跳至 X 页"输入框。
- 修正 AppPagination：彻底移除 » 后 antd 保留占位间距导致页码间空档突兀；按主流设计恢复静态"…"省略号（无边框背景、不可点、悬停无变化），填补截断位置。最终形态：‹ 1 … 3 4 5 … 7 › 跳至[ ]页。

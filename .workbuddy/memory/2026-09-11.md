## 附件上传报"附件内容与文件格式不匹配"排查
- 报错来源：backend/src/main/java/com/xqfx/requirements/requirement/AttachmentService.java:47 的魔数校验 hasValidFileSignature（jpg 要求文件头 FF D8 FF）。
- 前端 FormData 原样上传无转码；扩展名与 Content-Type 校验已通过（否则报"不支持的附件格式"），故根因是文件内容不是真 JPEG（典型：WebP/PNG 改名 .jpg，常见于微信保存图）。
- 结论：校验逻辑按设计工作，文件本身扩展名与实际格式不符。
- 更正：本项目后端实际为 Java Spring Boot（backend/，端口 8080），此前记忆中的 FastAPI 有误。
## 附件上传"内容优先"改造已实施
- AttachmentService.java：删除 matchesContentType/hasValidFileSignature，新增 detectActualFormat（魔数识别 jpg/png/gif/webp/pdf/ole/zip 分支），图片与 PDF 按真实格式落库（storedName/ContentType 用真实值，originalName 不变）；OLE（doc/xls）仍要求声明扩展名为 doc/xls；普通 ZIP 拒绝；无法识别报"无法识别附件内容格式"。
- 前端 RequirementEditor.vue / RequirementForm.vue：上传失败透出后端 message（复用 useApiError.ts 的 apiErrorDetails）。
- 新增测试 AttachmentServiceFormatTest（9 用例全通过）。本机无 mvn，采用手动编译：find .m2 生成 classpath（需 cygpath -m 转 C:/ 风格）+ javac @argfile + junit-platform-launcher Runner；junit 用 5.11.4 最小集，运行时需追加全量 .m2 cp。
- 更正：backend 实际是 Java Spring Boot（端口 8080），非 FastAPI。
## 项目快速启动已修复并验证
- 根因：用户在仓库根目录执行 npm run dev（package.json 在 frontend/ 下）；且 MySQL（phpstudy D:/phpstudy_pro/Extensions/MySQL5.7.26）与后端未启动。
- 本地环境事实：MySQL root 与 requirements_app 账号密码相同（见本机 start-backend.cmd，不入库）；库 requirements_platform（1022 条需求）；无 mvn；jar（2026-08-11）是旧构建，不含附件修复。
- 新增：backend/run-backend.args（按 jar 内 BOOT-INF/classpath.idx 精确重建的 62 个依赖 + target/classes，不能混入 src/main/resources 否则 Flyway 回调重复报错）；backend/start-backend.cmd（内置 DB_PASSWORD，java @run-backend.args RequirementsApplication，两者均已 gitignore）。
- 验证：后端 7.6s 启动 /api/health UP；前端 vite 5173，代理 /api→8080 端到端 OK。
- 注意：全量 .m2 jar 做 classpath 会因旧版 slf4j 1.7 与 Boot3 logback 冲突启动失败；Docker Desktop 未运行（MySQL 实际是 phpstudy 本机服务）。

## 附件粘贴上传已实施（change: add-attachment-paste-upload）
- 新建 frontend/src/composables/useClipboardAttachments.ts（244 行）：ClipboardEvent → { files, remoteImageUrls, rejectedReasons }，三路解析（items / files / text-html）。
- 关键工程点：
  - items.getAsFile() 与 getData('text/html') 必须同步调用（Firefox/Safari 部分版本事件返回后清空 DataTransferItemList）。
  - HTML 解析用 DOMParser 独立文档（inert），不用 v-html / innerHTML，杂讯零新增；SVG 一律拒绝（存储型 XSS 载体）。
  - 远程图片（http/https/file/cid/ftp）只列 URL 不下载，从根源规避 SSRF / 版权 / Referer 泄露。
  - 文件命名：位图 blob → 截图-YYYYMMDD-HHmmss[-index].<ext>；HTML data URL → 粘贴图片-YYYYMMDD-HHmmss-index.<ext>；clipboardData.files 保留原名。
  - 单次粘贴上限 20 个文件 / 10 MB HTML（防 Word 粘贴洪水与 base64 解码阻塞主线程）。
- 粘贴监听焦点分流：
  - RequirementForm.vue 挂在根 <section>（Ant Design Vue <Form> 组件 ref 拿到的是组件实例而非 DOM，无法 .contains()）。
  - RequirementEditor.vue 挂在原生 <form class="requirement-editor-form">；附件预览 Modal 为 form 兄弟节点，Modal.confirm 默认 teleport 到 body，两者均不会冒泡到 form。
  - 分流三步：容器外 return / 仅纯文本 return / 否则 preventDefault + addFiles + 高亮 1.2s + scrollIntoView。
- 顺带修复：RequirementEditor.vue 附件限制从 20 MB 纠正为 100 MB（addFiles 内部 + 提示文案），与填写页及 spec "附件客户端校验规则" 一致。
- 零后端改动：粘贴产生的 File 与点击 / 拖拽产生的 File 同构，走现有 POST /api/requirements/{id}/attachments，魔数校验天然覆盖截图 PNG/JPG/WebP。
- 验证：vue-tsc -b 零类型错误；vite build 3313 modules / 13.86s，JS gzip 545.87 kB（增量 < 5 KB），CSS gzip 10.93 kB。
- 测试基础设施：项目无 vitest / Playwright，本次不引入；单元测试与 E2E 作为后续独立立项（tasks.md Group 6/7 已保留完整用例清单）。spec.md 中所有 WHEN/THEN 场景由 Group 8 手工验证清单（15 项）覆盖。
- openspec 工件：openspec/changes/add-attachment-paste-upload/{proposal,design,tasks}.md + specs/requirement-attachments/spec.md（89 任务 / 9 requirements / 30+ scenarios），openspec validate --strict 通过。

## 截图 AI 识别已实施（change: add-image-ai-recognition）
- 新增 POST /api/ai/analyze-image（multipart file）：魔数校验（png/jpg/webp）→ 10MB 上限 → base64 data URL → OpenAI 多模态 content 数组 → 复用 parseAndValidate；图片不落盘不入日志。
- **mimo-v2.5 实测支持视觉**：canvas 生成的含中文需求文字截图经 /api/ai/analyze-image 返回 张三/标题/内容/typeId=12（字典匹配生效）；UI 端"AI 已识别并填充 3 个字段"。
- 视觉兜底实测触发：仅魔数的伪造 png 被厂商 4xx 拒绝（错误体含视觉关键词），mapVisionError 映射为"当前激活模型可能不支持图片识别…"引导文案。
- mapVisionError 关键词表：image / vision / visual / multimodal / 图片 / 视觉（仅 4xx 映射，5xx 不映射）。
- 纯函数可测性模式：mapVisionError / detectImageContentType / checkImageSize 均为 static 纯函数，15 个单测全绿（.codex-tmp/run-aiconfig-tests.ps1 + TestRunner.java，junit 5.11.4）。  <!-- 更正：同日后续用户决策删除全部新增测试文件与运行器（项目维持零新增测试），并 git rm 误提交的 node_modules/.vite/vitest results.json 缓存；验证以 browser-use 实测证据为准 -->
- AI 面板与附件区 paste 隔离：AI 面板 .ai-import-body 监听 paste，取到 image blob 后 preventDefault + stopPropagation（阻断冒泡到根 section 附件监听）；实测双向隔离生效。
- pwsh 坑：java -Dfile.encoding=UTF-8 在 pwsh 脚本中被拆参导致 ClassNotFoundException，JDK21 默认 UTF-8 直接省略该参数。
- browser-use 坑：evaluate_script 工具有 15s 超时，长轮询需拆成"触发"+"稍后读取"两次调用；ant message 停留约 3s，连续测试会读到旧 message 造成误判。
- 待补：IDE Browser 视图被隐藏时 take_screenshot 报 NATIVE_BROWSER_VIEWPORT_UNAVAILABLE，需用户重开 Browser 视图后补拍 5.8 证据截图。

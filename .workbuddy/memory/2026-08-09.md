# 2026-08-09

- 新建 docs/系统管理页视觉设计稿.html：将版本管理融合进系统管理。采用左右双栏主从布局——左栏系统列表（筛选条：名称/负责人/状态 + 卡片式系统条目含负责人/协助人/版本数/需求数/操作），右栏为选中系统的版本面板（新增版本 + 版本行含状态/需求数/操作）。侧边栏移除"版本管理"入口。底部附新增/编辑系统弹窗样式示意。设计语言与详情页/编辑页统一（蓝 #1677ff、圆角卡片）。
- 权限改造需求分析（未改代码）：用户拟将角色从二值(管理员/普通)扩为三角色——普通用户(仅填写/编辑/删除自己的需求，仅见填写需求+需求列表页，不可更新进度，不见通知)、需求处理员(=现普通用户权限)、管理员不变。现状关键点：users.admin_role 为 BOOLEAN；AuthInterceptor 仅拦 /api/users、/api/ai-config、/api/dictionaries，无资源级属主校验；系统/版本接口对全员开放。已向用户提出 6 个待确认问题（"自己的需求"定义、删除状态限制、列表可见范围、通知屏蔽副作用、处理员可否管系统/版本、工作台与后端拦截）。
- 三角色权限方案已定稿（用户确认 6 项决策）：普通用户仅见填写需求+需求列表(仅自己的)、可编辑自己需求、仅可删除草稿/待评估状态、不可为处理人/系统负责人、不见通知与工作台；需求处理员=原普通用户权限+可管系统/版本、可任系统负责人；管理员不变。新建 docs/人员管理页视觉设计稿.html（三角色说明卡片、人员列表、角色三选弹窗、三角色侧边栏对比）。待实施：users.admin_role→role 枚举迁移(存量非管理员→HANDLER)、AuthInterceptor 角色维度、RequirementService 属主+状态校验、通知/工作台/系统接口对普通用户拦截、前端菜单与按钮按角色显隐。
- 方案已落盘为 docs/三角色权限改造方案.md（含决策记录、权限矩阵、前后端改造点、实施顺序、风险提示）。
- 人员管理页设计稿按用户要求移除顶部角色说明卡片与底部三角色菜单对比区，仅保留列表+三选角色弹窗。
- 新建统一刷新机制：docs/页面刷新机制方案.md + docs/页面刷新交互设计稿.html（可交互演示）。核心：顶栏/侧边栏固定不刷新，反馈仅在 main 内容区；有数据刷新=2px 顶部进度条+内容 55% 透明+刷新图标旋转；无数据首载=骨架屏；废弃各页面散落的 Spin 文案，新增 PageRefreshBar/ContentSkeleton 组件，usePageRefresh 输出 refreshing/loaded 状态。
- 新建 docs/需求列表页视觉设计稿.html：精简筛选区。原 8 字段平铺+提示条 → 页签(未完结/全部 带计数)+关键词搜索(⌘K)+筛选面板(选择即生效，取消查询按钮)+已选条件 chips(可单个移除/清空，筛选按钮角标计数)。首屏筛选区高度约 260px→110px。
- 工作台改版（用户已确认 3 项决策：概览页=全平台数据且处理员+管理员可见；图表维度=系统/状态/紧急程度；协助区为空自动隐藏）。新建 docs/待办工作台视觉设计稿.html（负责区独占整行，协助区有数据时以 1/3 窄栏出现，含切换演示；原 12 格统计卡移出工作台）。新建 docs/需求概览页视觉设计稿.html（4 KPI 卡 + 系统分布环形图 + 状态分布环形图 + 紧急程度横条图，纯 SVG 实现，侧边栏新增"需求概览"入口）。
